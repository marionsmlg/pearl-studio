[build]
  command = "npm run build"
  publish = "dist"

# Ignore documentation files during secrets scanning
[build.environment]
  SECRETS_SCAN_OMIT_PATHS = "doc/**,*.md,README.md"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  directory = "netlify/functions"
